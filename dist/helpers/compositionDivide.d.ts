import { IComponent } from '../types';
export declare const getComponents: (componentVue: IComponent) => number[][];
export declare const divide: (componentVue: IComponent, groupSingle?: boolean) => IComponent[];
